# JFLAP 7.1 conformance evidence

## Automated vendor corpus

The repository retains unchanged vendor-authored examples under `packages/jff/__tests__/fixtures/official`. Their source URLs, retrieval dates, and independently specified languages are recorded in the fixture README. `conformance.test.ts` checks representative execution outcomes and graph-preserving export without using Jauto's serializer to establish the expected languages.

Current corpus:

- `ex1.3a.jff`: nondeterministic finite automaton from the JFLAP book files.
- `ex5.1.jff`: pushdown automaton from the JFLAP book files.
- `ex9-anbncn.jff`: single-tape Turing machine from the JFLAP book files.
- `pdaexample.jff`: pushdown automaton from the official construction tutorial.

## Jauto export reopened by JFLAP 7.1

Verified on 2026-09-06 with Java 8 and the official `JFLAP7.1_With_Source.jar` (SHA-256 `700D324AB1EFE9FBFCC71DA70B63FF52CC148D5FA0C91A6EC9D681D0741EFAB3`).

The test document was generated by Jauto with four states whose internal IDs were `s0`, `s1`, `0`, and `state-custom`, plus transitions connecting all four. Jauto exported unique state IDs `1`, `2`, `0`, and `3` and consistently remapped the transition endpoints. JFLAP's own `file.XMLCodec.decode` reopened the result as `automata.fsa.FiniteStateAutomaton` without an exception.

This verifies structural acceptance by the target application. The automated serializer tests separately check XML validity, unique integer IDs, endpoint consistency, semantic values, annotations, and layout round trips. Visual inspection in the JFLAP editor remains appropriate when changing exported layout fields.
